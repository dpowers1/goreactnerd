type Product = {
  id: number,
  title: string,
  price: number,
  images: string[],
  description: string,
  category: string,
  rating: number,
  stock: number,
  brand: string,
  discountPercentage: number,
  thumbnail: string  
}

export default Product;